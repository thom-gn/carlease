# Set the server port
server.port=8080

# Set the context path
server.servlet.context-path=/myapp

# Set the maximum number of threads
server.tomcat.max-threads=200

# Set the connection timeout
server.tomcat.connection-timeout=20000

#store it on drive so data is not lost every code change
spring.datasource.url=jdbc:h2:file:./data/mydb
#spring.datasource.url=jdbc:h2:mem:testdb 
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
# Hibernate dialect for H2
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Disable automatic DDL generation
spring.jpa.hibernate.ddl-auto=update

# GitHub OAuth2 Client Configuration
spring.security.oauth2.client.registration.github.client-id=Ov23liaPmOxQCC9V0G6S
spring.security.oauth2.client.registration.github.client-secret=53878edfa34fc22ce9c2f1a992d1c08608c5495e
spring.security.oauth2.client.registration.github.scope=repo,read:user
spring.security.oauth2.client.registration.github.redirect-uri=http://localhost:8080/myapp/login/oauth2/code/github
spring.security.oauth2.client.registration.github.authorization-grant-type=authorization_code
spring.security.oauth2.client.registration.github.client-name=GitHub
spring.security.oauth2.client.registration.github.client-authentication-method=client_secret_basic

# GitHub OAuth2 Provider Configuration
spring.security.oauth2.client.provider.github.authorization-uri=https://github.com/login/oauth/authorize
spring.security.oauth2.client.provider.github.token-uri=https://github.com/login/oauth/access_token
spring.security.oauth2.client.provider.github.user-info-uri=https://api.github.com/user
spring.security.oauth2.client.provider.github.user-name-attribute=login
spring.security.oauth2.client.provider.github.jwk-set-uri=https://github.com/login/oauth/jwks

# Resources Server JWT Configuration
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://github.com/login/oauth/issuer
logging.level.org.springframework.security=DEBUG

logging.level.com.assessment.sogeti.carlease=DEBUG
logging.level.org.springframework.web=DEBUG